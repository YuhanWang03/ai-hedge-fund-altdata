"""Evaluation harness for the agent loop, the router and the assist layers.

Everything measured here runs against recorded observations, so a score depends
only on the model and the code under test — never on what the market did today.
"""
